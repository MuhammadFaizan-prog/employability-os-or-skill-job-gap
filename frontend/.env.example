# EmployabilityOS — Supabase (skilljob project)
# Copy to .env and fill from your Supabase project: Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: only for running seed from repo root (npm run seed:step2). Never commit. Do not use VITE_ prefix.
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
